<div class="dashboard-header">
<h1>Welcome to Your dashbaord, <%= current_user.username %>!</h1>
</div>

<hr>

<%# added the entire card clickable %>
<div>
  <h2>Your bookmarks:</h2>
  <ul class="container-fluid">
    <% if @bookmarks.empty? %>
      <p>No bookmarks created.</p>
    <% else %>
      <% @bookmarks.each do |bookmark| %>
        <%= link_to restroom_path(bookmark) do %>
          <div class="card-body">
            <% bookmark = Restroom.find_by(id: bookmark.restroom_id) %>
            <%= bookmark.name %>
          </div>
        <% end %>
      <% end %>
    <% end %>
  </ul>
</div>

<hr>

  <div>
   <h2>Your restrooms:</h2>
    <ul class="container-fluid">
      <% if @restrooms.empty? %>
        <p>No restrooms created.</p>
      <% else %>
        <% @restrooms.each do |restroom| %>
        <div class="card-body">
          <% restroom = Restroom.find_by(user_id: @user.id) %>
          <%= link_to restroom.name, restroom_path(restroom) %>
        </div>
        <% end %>
      <% end %>
    </ul>
  </div>

<hr>

<div>
  <h2>Your reviews:</h2>
  <ul class="container-fluid">
    <% if @reviews.empty? %>
      <p>No reviews created.</p>
    <% else %>
      <% @reviews.each do |review| %>
      <div class="card-body">
        <% restroom = Restroom.find_by(id: review.restroom_id) %>
        <li>
          <%= link_to "#{review.comment} - #{restroom.name}", restroom_path(restroom) %>
        </li>
      </div>
      <% end %>
    <% end %>
  </ul>
</div>
